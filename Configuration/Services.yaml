services:
  _defaults:
    autowire: true
    autoconfigure: true
    public: false

  LIA\LiaHyphenator\:
    resource: '../Classes/*'

  # Fetched via GeneralUtility::makeInstance() from the ViewHelper.
  # Must be public for makeInstance() to resolve constructor dependencies.
  LIA\LiaHyphenator\Services\HyphenatorService:
    public: true
